{
  "name": "irs-guia",
  "version": "0.1.0",
  "description": "Guia pedagógico interactivo do IRS português — motor de cálculo + UI editorial.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/wicg-file-system-access": "^2023.10.7",
    "@vitest/ui": "^1.2.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.12",
    "vitest": "^1.2.0"
  },
  "dependencies": {
    "marked": "^18.0.4"
  }
}
